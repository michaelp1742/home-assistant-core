{
  "config": {
    "abort": {
      "already_configured": "[%key:common::config_flow::abort::already_configured_device%]",
      "already_in_progress": "[%key:common::config_flow::abort::already_in_progress%]",
      "no_devices_found": "[%key:common::config_flow::abort::no_devices_found%]"
    },
    "error": {
      "cannot_connect": "[%key:common::config_flow::error::cannot_connect%]",
      "invalid_auth": "[%key:common::config_flow::error::invalid_auth%]",
      "invalid_key_format": "The offline key must be a 32-byte hex string.",
      "invalid_key_index": "The offline key slot must be an integer between 0 and 255.",
      "no_longer_in_range": "The lock is no longer in Bluetooth range. Move the lock or adapter and try again.",
      "unknown": "[%key:common::config_flow::error::unknown%]"
    },
    "flow_title": "{name}",
    "step": {
      "integration_discovery_confirm": {
        "description": "Do you want to set up {name} over Bluetooth with address {address}?"
      },
      "key_slot": {
        "data": {
          "key": "Offline Key (32-byte hex string)",
          "slot": "Offline Key Slot (Integer between 0 and 255)"
        },
        "description": "Enter the key for the {title} lock with address {address}. If you are using the August or Yale cloud integration to obtain the key, you may be able to avoid this manual setup by reloading the August or Yale cloud integration while the lock is in Bluetooth range."
      },
      "reauth_validate": {
        "data": {
          "key": "[%key:component::yalexs_ble::config::step::key_slot::data::key%]",
          "slot": "[%key:component::yalexs_ble::config::step::key_slot::data::slot%]"
        },
        "description": "Enter the updated key for the {title} lock with address {address}. If you are using the August or Yale cloud integration to obtain the key, you may be able to avoid manual re-authentication by reloading the August or Yale cloud integration while the lock is in Bluetooth range."
      },
      "user": {
        "data": {
          "address": "Bluetooth address"
        },
        "description": "Select the device you want to set up over Bluetooth."
      }
    }
  },
  "entity": {
    "lock": {
      "secure_mode": {
        "name": "Secure mode"
      }
    },
    "sensor": {
      "battery_voltage": {
        "name": "Battery voltage"
      },
      "unread_events": {
        "name": "Unread activity records"
      }
    }
  },
  "exceptions": {
    "device_not_advertising": {
      "message": "{local_name} ({address}) is not advertising yet: {reason}"
    }
  },
  "options": {
    "step": {
      "init": {
        "description": "Choose what to change for {title}.",
        "menu_option_descriptions": {
          "lock_options": "Always connected, and the features Home Assistant offers for this lock."
        },
        "menu_options": {
          "lock_options": "Lock options"
        },
        "title": "Options"
      },
      "lock_options": {
        "data": {
          "activity_count": "Unread activity count",
          "always_connected": "Always connected",
          "auto_lock": "Auto-Lock",
          "battery_reporting": "Battery reporting",
          "door_sense": "DoorSense",
          "secure_mode": "Secure mode",
          "unlatch": "Unlatch"
        },
        "data_description": {
          "activity_count": "On creates a diagnostic sensor for how many activity records remain on the lock. Off removes the sensor and stops the reads.",
          "always_connected": "Keeps the lock connected to Home Assistant over Bluetooth, for a lock that does not push updates in its advertisements or for faster lock operation.",
          "auto_lock": "On adds the Auto-Lock setting page to these options. Off removes it.",
          "battery_reporting": "On creates the battery sensors and reads the battery level from the lock. Off removes the sensors and stops the reads.",
          "door_sense": "On creates the door sensor and asks the lock for the door state, even on a model not known to have a door sensor. Off removes the sensor and stops the requests.",
          "secure_mode": "On creates the Secure mode lock entity, enabled, which locks the lock in secure mode. Not set creates it disabled. Off removes it.",
          "unlatch": "On adds the Open action to the lock entity, which pulls the latch back for a settable duration. Also adds the Unlatch hold time setting page. Off removes it."
        },
        "description": "Not set means you have made no choice for the feature. Library default means you chose to leave it to the library. Both send nothing to the library, so its own answer holds for either. On and Off are sent to the library. A feature that Home Assistant adds, the Open action, the unread activity count and Auto-Lock, appears only when it is set to On. A feature whose key the installed library does not take is not offered, and the setup log records which keys were taken.",
        "title": "Lock options"
      }
    }
  },
  "selector": {
    "feature_state": {
      "options": {
        "default": "Library default",
        "off": "Off",
        "on": "On",
        "unconfigured": "Not set"
      }
    }
  }
}
